import grayCode from '../../code/array/grayCode.js';
test('grayCode(3)', () => {
  expect(grayCode(3)).toEqual([0, 1, 3, 2, 6, 7, 5, 4]);
});

// test('canplaceFlowers.test:[1, 0, 0, 0, 1], 1', () => {
//   expect(canplaceFlowers([1, 0, 0, 0, 1], 1)).toBe(true);
// });
